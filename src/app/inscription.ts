import { Evenement } from "./evenement";

export class Inscription {
    numero: number;
    nomClient: string
    prenomClient: string
    telClient: number;
    cinClient: number;
    evenement: Evenement;
}
